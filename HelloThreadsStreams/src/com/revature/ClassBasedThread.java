package com.revature;

//This custom Thread extends the Thread Class in order to override the run() method
public class ClassBasedThread extends Thread{

    @Override
    public void run(){
        //We could have defined a custom thread functionality here...
        //But we'll use the InterfaceBasedThread instead
    }

}
